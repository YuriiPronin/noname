import React from 'react';
import { Typography } from '@mui/material';

export const AccountPage = () => {
  return (
    <Typography sx={{fontSize: '35px', color: 'red', textAlign: 'center'}}>
      !You must log in. 
      But, unfortunately, the authorization has not yet been implemented!
    </Typography>
  );
};
